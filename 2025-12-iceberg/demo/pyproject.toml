[project]
name = "iceberg-talk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.1.0",
    "marimo>=0.18.1",
    "pyiceberg[datafusion,sql-sqlite]>=0.10.0",
]
